#include <bits/stdc++.h>

using namespace std;

long long int n;

int main() {
  cin.tie(0) -> ios::sync_with_stdio(0);

  cin >> n;

  cout << (n * (n - 1)) / 2 << "\n2\n";

  return 0;
}