#include <bits/stdc++.h>

using namespace std;

long long int n;

int main() {
  cin.tie(0) -> ios::sync_with_stdio(0);

  cin >> n;

  cout << (n * (n - 1) * (n - 2)) / 6 << "\n3\n";

  return 0;
}